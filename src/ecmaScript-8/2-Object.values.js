//              Object.values()
// Devuelve un array de strings con los valores de cada
// propiedad.

const data = {
    frontend: 'Daniel',
    backend: 'Laura',
    desing: 'Sebastian',
};

const values = Object.values(data);
console.log(values);